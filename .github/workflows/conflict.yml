name: MY CONFLICT COMMIT
on:
  push:
    branches:
    - master
jobs:
  hello-jobs:
    runs-on: ubuntu-latest
    steps:
    - name: "MY COMMIT FROM MASTER"
      run: echo "WELCOME AGAIN MAN HAVE A NICE DAY"
